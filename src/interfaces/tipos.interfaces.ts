export interface Tipo {
  tipo: string;
}

export interface TipoAdd {
  tipo: string;
}

export interface TipoList {
  id: string;
  tipo: string;
  createdAt: Date;
  updateAt: Date;
}

export interface tipoProps {
  id: string;
  tipo: string;
}
